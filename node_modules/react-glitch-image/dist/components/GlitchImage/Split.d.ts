import { default as React } from 'react';

export declare const useSplitImage: (src: string, splitSize: number) => {
    pieces: React.ReactElement<({ image, height, index }: {
        image: HTMLImageElement;
        height: number;
        index: number;
    }) => import("react/jsx-runtime").JSX.Element, string | React.JSXElementConstructor<any>>[] | null;
};
